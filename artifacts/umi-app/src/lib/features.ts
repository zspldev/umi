/**
 * Feature flags — flip these to enable/disable work-in-progress features.
 * Each flag is a single boolean; search for the constant name to find all guarded UI/routes.
 */

/** AI Language Tutor ("Learn" mode). Set to true when ready to re-enable. */
export const TUTOR_ENABLED = false;
